server:
  port: 8082

spring:
  application:
    name: JwtService

eureka:
  instances:
    prefer-ip-address: true
  client:
    fetch-registry : true
    register-with-eureka: true
    service-url :
      defaultZone : http://localhost:8761/eureka

# http://localhost:8761/